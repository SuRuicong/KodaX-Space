{
  "name": "kodax-space",
  "version": "0.1.5",
  "description": "KodaX Space — Provider-neutral, local-first AI agent desktop client (KodaX 生态桌面客户端)",
  "private": true,
  "type": "module",
  "license": "Apache-2.0",
  "author": "icetomoyo <icetomoyo@gmail.com>",
  "repository": {
    "type": "git",
    "url": "https://github.com/icetomoyo/KodaX-Space.git"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "npm run build:packages && npm run build:renderer && npm run build:main && npm run build:pack",
    "build:packages": "npm run build -w @kodax-space/space-ipc-schema && npm run build -w @kodax-space/space-ui-kit",
    "build:renderer": "npm run build -w @kodax-space/desktop",
    "build:main": "node scripts/build-main.mjs",
    "build:pack": "electron-builder -p never",
    "build:win": "node scripts/gen-icon.mjs && npm run build:smoke && electron-builder --win -p never",
    "build:mac": "node scripts/gen-icon.mjs && npm run build:smoke && electron-builder --mac -p never",
    "build:linux": "node scripts/gen-icon.mjs && npm run build:smoke && electron-builder --linux -p never",
    "build:smoke": "npm run build:packages && npm run build:renderer && npm run build:main",
    "smoke:pack": "node scripts/smoke-pack.mjs",
    "typecheck": "tsc -p apps/desktop && tsc -p apps/desktop/electron",
    "lint": "eslint . --ext .ts,.tsx,.mjs --max-warnings 0",
    "lint:fix": "eslint . --ext .ts,.tsx,.mjs --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "clean": "node scripts/clean.mjs",
    "link:kodax": "node scripts/link-kodax.mjs",
    "unlink:kodax": "node scripts/link-kodax.mjs --unlink",
    "test": "npm run test --workspaces --if-present",
    "e2e": "npm run build:smoke && npm run e2e:run",
    "e2e:run": "playwright test",
    "e2e:headed": "playwright test --headed",
    "postinstall": "electron-builder install-app-deps || true"
  },
  "dependencies": {
    "@kodax-ai/kodax": "^0.7.42"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@types/node": "^22.19.19",
    "@typescript-eslint/eslint-plugin": "^8.18.0",
    "@typescript-eslint/parser": "^8.18.0",
    "concurrently": "^9.1.0",
    "electron": "^33.2.0",
    "electron-builder": "^25.1.8",
    "esbuild": "^0.24.0",
    "eslint": "^9.16.0",
    "eslint-plugin-react": "^7.37.0",
    "eslint-plugin-react-hooks": "^5.1.0",
    "playwright": "^1.60.0",
    "prettier": "^3.4.0",
    "tsx": "^4.22.0",
    "typescript": "^5.7.0",
    "wait-on": "^8.0.1"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
