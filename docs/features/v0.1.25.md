# v0.1.25 - FEATURE_107 KodaX 0.7.54 SDK Catch-up + FEATURE_108 Composer Dropped File References

> Design batch: F107 + F108
> Status: Completed
> SDK baseline: `@kodax-ai/kodax@0.7.54`; current installed baseline is `@kodax-ai/kodax@0.7.56` with the addendum below.
> Started: 2026-06-23

## Feature_107 KodaX 0.7.54 SDK Catch-up: Learning, Recovery, Extensions

### 1. Goal

`0.7.54` is not only a dependency bump. It adds a new procedural-learning review loop, session recovery seeds, filesystem extension discovery/runtime composition, GLM provider-model refresh, and manual self-knowledge updates. Space should expose the user-visible parts quickly, while keeping destructive or cross-runtime changes behind reviewable gates.

This feature turns the former `v0.1.25` patch reserve into the SDK catch-up lane before F103 starts at `v0.1.26`.

### 2. Match Matrix

| SDK 0.7.54 capability | Space match | Plan | Status |
| --- | --- | --- | --- |
| npm package `@kodax-ai/kodax@0.7.54` | Root + desktop dependency | Bump `package.json`, `apps/desktop/package.json`, lockfile, and SDK probe expectations. | Done |
| GLM provider refresh | Provider catalog + model caps | Make `zhipu-coding` default to `glm-5.2`; expose `glm-5.2`, `glm-5-turbo`, `glm-4.7`; align renderer context-window hints. | Done |
| FEATURE_224 procedural learning store | Slash command review surface | Add `/learn pending|diff|approve|reject` backed by SDK store; add `/skill pending`, `/workflow pending`, `/memory pending` filtered views. | Done (Phase A) |
| Skill approval safety | SDK `approveStoredLearningProposal` | Keep approval behind explicit slash command; require `--ack-impact` when SDK reports consumer impact; show diff/apply-plan before approval. | Done (Phase A) |
| Learning ledgers | Slash diagnostics | Surface `readSkillUsageLedger` and `readSkillTrustLedger` through `/learn ledger` and `/skill ledger`. Rich skill-detail UI remains a later UX enhancement. | Done |
| Completed-turn learning | Real session lifecycle | Call `recordCompletedTurnLearning` after successful assistant turns with captured user/assistant evidence; keep candidates empty until an explicit candidate generator supplies durable proposals, so Space never writes skills from hidden background paths. | Done (candidate-gated) |
| Session recovery seed builder | Slash recovery preview | Use `isSessionRecoveryCandidateError` for `/recover candidate`; build `/recover seed` from persisted messages, lineage, and artifact ledger using SDK `buildRecoverySeed`. | Done |
| Recovery prompt normalization | Recovery UX | Expose `/recover prompt [reason]` through SDK `normalizeRecoveryPrompt`, and pass optional reasons into `/recover seed`. | Done |
| Filesystem extension discovery | Extensions diagnostics | Use `discoverDefaultExtensions`/`discoverExtensionsInDirectoryDetailed` from `/extensions sdk`, including skipped entries and active runtime diagnostics. | Done |
| Runtime composition | Real KodaX session runtime | Add a shared SDK extension runtime loader; `/extensions sdk load` explicitly activates discovered extensions, and `KODAX_SPACE_ENABLE_SDK_EXTENSIONS=1` injects the runtime into `RealKodaXSession`. Default behavior remains discovery-only. | Done (opt-in) |
| Manual topic `extensions` | Space manual overlay | Add a Space-specific extensions/manual topic that explains MCP/MCPB, filesystem extensions, `/extensions`, and the opt-in runtime policy without overriding SDK internals. | Done |
| SDK hygiene | Tests + capability ledger | Keep SDK probe, provider catalog tests, model-cap tests, slash tests, and this feature doc as the rebaseline evidence. | Done |

### 3. Implementation Phases

Completed SDK parity surface:

- Upgrade Space to `@kodax-ai/kodax@0.7.54`.
- Align GLM provider defaults and renderer model context caps.
- Add slash learning inbox commands using SDK store APIs.
- Add skill usage/trust ledger diagnostics via `/learn ledger` and `/skill ledger`.
- Add recovery diagnostics and seed preview via `/recover candidate|prompt|seed`.
- Add SDK filesystem-extension discovery via `/extensions sdk` and explicit runtime activation via `/extensions sdk load`.
- Inject the SDK extension runtime into real sessions only when `KODAX_SPACE_ENABLE_SDK_EXTENSIONS=1` is set.
- Add Space manual coverage for extensions.
- Wire completed-turn learning recording after successful turns while keeping proposal creation candidate-gated.

Future rich UI enhancements:

- Add a Learning Inbox panel with proposal counts, kind filters, apply-plan diff preview, and approval/rejection controls.
- Add skill usage/trust ledger views to the Skills surface.
- Add a first-class recovery-session fork action once Space exposes a persisted seed-to-session writer.

### 4. Acceptance Criteria

- `/learn pending` lists SDK learning proposals for the current project.
- `/learn diff <id>` shows proposal detail and any safe apply plan without applying it.
- `/learn reject <id> [reason]` updates the SDK proposal store.
- `/learn approve <id>` delegates to SDK safety gates and reports blocked consumer impact clearly.
- `/skill pending`, `/workflow pending`, and `/memory pending` show filtered proposal queues.
- GLM defaults and context hints match SDK `0.7.54` model lineup.
- Future recovery and extension work is tracked here rather than hidden as ad hoc SDK drift.

### 5. Verification

Verification:

- `node --test --import tsx/esm electron/test/slash-builtin.test.ts`

Broader release verification should additionally run:

- `npm run typecheck`
- provider/catalog/model-cap tests
- SDK probe tests
- build smoke before tagging

---

## Feature_108 Composer Dropped File References

### 1. Goal

Let users drag files into the composer and see durable, sendable references directly in the input draft. This is the Space-owned UI and shell integration layer for file references; it does not try to define the full KodaX SDK file/video artifact contract.

This closes the immediate desktop gap after OC-31 image paste: users can now drop local files, directories, and supported image files onto the input surface instead of manually typing paths.

### 2. Scope

Completed Space-owned behavior:

- Resolve a user-dropped browser `File` to an OS path through Electron `webUtils.getPathForFile` exposed as a narrow preload bridge.
- Insert a safe project-relative reference as `@relative/path` when the file is inside the current project and the path can be represented safely.
- Insert an external markdown file link as `[name](<file://...>)` when the file is outside the current project or needs quoting/escaping.
- Show dropped file chips above the composer with remove actions that also remove the inserted reference from the draft.
- Cap pending file references to 32 per draft.
- For dropped PNG/JPEG/WEBP files, reuse the existing OC-31 image path: persist the image through `clipboard.saveImage`, show an image chip, and send it as a `kind: 'image'` input artifact.

### 3. Non-Goals and Boundaries

This feature deliberately does not claim the whole multimodal roadmap:

- Native clipboard image fallback is not implemented in Space yet. KodaX 0.7.56 now exposes public media helpers; Space still needs a main-process IPC bridge, sandboxed persistence, and renderer UX before this is user-facing.
- GIF animation preservation is not guaranteed. KodaX 0.7.56 includes `image/gif` in its media contract, but Space's current `clipboard.saveImage` sandbox persists only PNG/JPEG/WEBP.
- Video is not a structured Space artifact yet. KodaX 0.7.56 can report provider video capability state, but dropped videos are still referenced by path/link only until Space defines capture, persistence, preview, and provider fallback behavior.
- Ordinary non-image files are not sent as structured `kind: 'file'` artifacts. KodaX 0.7.56 still reports file artifacts as unsupported for this contract; they remain prompt-visible references that the agent can reason about or open through existing tools when appropriate.
- This feature does not add a file picker or attach menu redesign.

### 4. Design Notes

The preload bridge is intentionally narrow:

```ts
getPathForFile(file: File): string | null
```

Renderer code never receives Electron primitives. The only supported path source is a real user-provided drag/drop `File` handle, which keeps this distinct from arbitrary renderer-originated path injection.

Reference formatting follows two rules:

1. Project-local safe paths prefer `@relative/path`, matching existing KodaX/Space path reference semantics.
2. External or hard-to-tokenize paths become encoded `file://` markdown links so spaces, Windows drive letters, UNC paths, and special characters survive insertion.

Images use the stricter existing artifact path:

- accepted types: `image/png`, `image/jpeg`, `image/webp`,
- max size: existing OC-31 image paste cap,
- persisted in the Space clipboard sandbox,
- sent through `session.send({ artifacts: [{ kind: 'image', path, mediaType }] })`.

### 5. Acceptance

- Dropping a project file inserts an `@relative/path` reference into the composer.
- Dropping an external file inserts a clickable markdown `file://` reference into the composer.
- Dropped references appear as removable chips and removing a chip removes the corresponding inserted reference.
- Dropping more than 32 files caps the draft and reports a local composer warning.
- Dropping PNG/JPEG/WEBP files both inserts file references and attaches image artifacts for the next send.
- Sending a draft with only dropped file references is allowed.
- Unsupported media types do not crash the composer; they remain path references unless a later structured artifact contract supports them.

### 6. Follow-Up Hooks

- Wire native clipboard image fallback using KodaX 0.7.56 `readAndNormalizeClipboardImage` / `persistImageAsBlock` through a main-process IPC boundary.
- Use KodaX 0.7.56 `getModelInputCapabilities` and `validateInputArtifactsForModel` to preflight image sends and explain provider/model limitations before the SDK throws.
- Add GIF direct-path preservation only after Space has a safe persistence/preview path that does not flatten animation into a static screenshot.
- Add a structured `kind: 'file'` artifact path only after KodaX SDK reports a supported file artifact contract and provider fallbacks.
- Add structured `kind: 'video'` support or video-frame extraction once Space defines the UX and KodaX/provider capability state is wired through Settings/composer affordances.
- Add richer attach menu affordances after the drag/drop baseline has proved stable.

### 7. KodaX 0.7.56 Addendum

KodaX 0.7.56 changes the F108 follow-up status from "SDK-gated" to "Space implementation needed":

- `@kodax-ai/kodax/media` is public and exports clipboard image reading/normalization, image artifact construction, temporary persistence helpers, model input capability checks, and artifact validation.
- `KodaXInputArtifact.source` now distinguishes `user-inline`, `clipboard`, `drag-drop`, and `file-picker`; Space preserves this source when sending persisted PNG/JPEG/WEBP artifacts.
- `KodaXInputArtifact.mediaType` is narrowed to SDK-owned image media types. Space keeps its current PNG/JPEG/WEBP sandbox and treats GIF as a later direct-path/persistence follow-up.
- Provider-neutral stop reason classification is handled inside KodaX runtime; Space has no new IPC requirement unless it later wants to surface paused/refused/truncated states explicitly.
- Ark/Kimi provider catalog updates are consumed from SDK `provider-capabilities.json`; Space's local renderer fallback now treats `kimi-k2.7-code` as a 256k model.

### 8. Implementation Evidence

- `apps/desktop/electron/preload.ts` exposes `getPathForFile` through Electron `webUtils.getPathForFile`.
- `apps/desktop/renderer/src/types/global.d.ts` types the bridge.
- `apps/desktop/renderer/src/shell/BottomBar.tsx` owns drag/drop state, reference formatting, chips, inline image reuse, and send enablement for reference-only drafts.
