# KodaX Space v0.1.0 — Alpha Foundation

> Target: 2026-Q2
> Theme: 在桌面跑通 KodaX session 全流程
> Status: Planned
> Definition of Done: 开发者能在 Space 里完成"打开项目 → 提问 → 看 tool 调用 → 改动落盘 → diff 审查"

## 版本范围

v0.1.0 是 KodaX Space 的**第一个 alpha**。目标是**架构验证**而非功能完备：

- 验证 Electron + in-process KodaX runtime 模型可跑（[ADR-003](../ADR/ADR-003-kodax-integration-in-process.md)）
- 验证 main → renderer 的 IPC schema 与事件流可承载 KodaX session
- 验证 Provider 配置 + Keychain + LLM 调用全链路打通
- 验证 Permission 弹窗能正确拦截危险操作
- 验证 Monaco diff 渲染能服务 review 工作流

**不在 v0.1.0 范围**：
- 内置终端（v0.1.1）
- Subagent 可视化（v0.1.1）
- MCP 管理 GUI（v0.1.1，但 MCP 通过 KodaX 内核 config 可用）
- Quick Ask popover（v0.1.2）
- Repointel UI（v0.1.2）
- 主题切换（v0.1.3）

## MVP 主流程

```text
启动 Space
  → Welcome 屏：选择主语言 + 配 1 个 provider（写 keychain）
  → 选项目目录
  → 自动新建 session
  → 输入 "Read package.json and summarize"
  → 看 tool call 卡片（read 工具）+ tool result
  → 看 LLM 回答流式渲染
  → 再问 "Find security issues in src/auth.ts and propose fixes"
  → 看 read → grep → write 工具链
  → write 工具触发 Permission 弹窗 → 用户 Allow
  → 在 File panel 看 diff
  → 任务完成，session 落盘到 ~/.kodax/sessions/<id>.jsonl
```

## 验收标准（版本级）

- [ ] Win 10/11 + macOS 14+ 启动安装包能跑（unsigned dev build）
- [ ] 至少 3 个 provider 工作：anthropic、zhipu-coding、openai
- [ ] 能完成上述 MVP 主流程不报错
- [ ] Permission 弹窗 100% 拦截 bash / write / edit / write-outside-project
- [ ] Session JSONL 与 KodaX CLI 完全兼容（CLI resume 同一 session 不报错）
- [ ] renderer bundle 不含 `@anthropic-ai/sdk` / `openai` / `runKodaX`（CI 校验）
- [ ] 冷启动 < 4s（开发包；优化留到 v0.1.5）

---

## FEATURE_001: Electron 工程骨架

> Status: **Completed** (2026-05-16, v0.1.0-alpha.0)
> 实际版本：Electron 33（≥ 30+ 基线满足）；npm workspaces；esbuild → CJS（main + preload）；Vite 6 → renderer。
> 安装包构件留 [FEATURE_010](#feature_010-跨平台安装包unsigned-dev) 处理；本 feature 只交付**可跑骨架** + smoke build。

### 需求概述

建立 KodaX Space 工程基底——Electron + Vite + React + TypeScript + Zustand。所有后续 feature 在此之上展开。

### 影响范围

- 全新仓库（`KodaX-Space/`）
- 新建：`apps/desktop/electron/{main.ts, preload.ts}`、`apps/desktop/renderer/{main.tsx, App.tsx}`
- 新建：`packages/space-ui-kit/`、`packages/space-ipc-schema/`
- 新建：根 `package.json`、`tsconfig.json`、`electron-builder.yml`
- 不动 KodaX / KodaX-private

### 技术方案

- Electron 30+（最新稳定版）
- Renderer：Vite + React 18 + TypeScript + Tailwind + shadcn/ui + Zustand
- Main：esbuild bundle，TypeScript 严格模式
- Preload：暴露最小白名单 IPC API 到 renderer（`window.kodaxSpace.*`）
- 工程：npm workspaces；CI: GitHub Actions（Win + macOS runner）
- 安全：contextIsolation = true、nodeIntegration = false、sandbox = true（renderer）、CSP `default-src 'self'`

### 接口契约

无外部 API；定义内部目录约定：

```typescript
// apps/desktop/electron/main.ts
const mainWindow = new BrowserWindow({
  webPreferences: {
    contextIsolation: true,
    nodeIntegration: false,
    sandbox: true,
    preload: path.join(__dirname, 'preload.js'),
  },
});

// apps/desktop/electron/preload.ts
import { contextBridge, ipcRenderer } from 'electron';
contextBridge.exposeInMainWorld('kodaxSpace', {
  invoke: (channel: string, payload: unknown) => ipcRenderer.invoke(channel, payload),
  on: (channel: string, cb: (event: unknown) => void) => ipcRenderer.on(channel, (_, p) => cb(p)),
});
```

### 实现步骤

1. `npm init` + 配 workspace（apps/* + packages/*）
2. 装 Electron + electron-builder + vite + react + tailwind + shadcn + zustand
3. 配 `tsconfig.json` 严格模式 + ESLint + Prettier
4. 写 main.ts + preload.ts + 最小 renderer（"Hello KodaX Space"）
5. 配 `npm run dev`：concurrently 跑 vite dev + electron with HMR
6. 配 `npm run build`：vite build → esbuild main → electron-builder package
7. 配 CI：Win + macOS 各 build 一次 smoke 测试
8. 安全审计：CSP / contextIsolation 测试

### 验收标准

- [x] `npm install` 干净通过（npm workspaces，726 包，0 警告 0 错误）
- [x] `npm run build:smoke` 干净通过：`dist-electron/{main.js,preload.js}` + `apps/desktop/dist/index.html` + 两个 packages dist 全产出
- [x] `npx electron dist-electron` 启动后窗口显示 React UI（self-check 页：preload bridge ✓ / platform / 无 node 泄露）— smoke launched 2026-05-16
- [x] CSP header 在 main 进程 `session.webRequest.onHeadersReceived` 注入；dev/prod 双套
- [x] `webPreferences` 满足 contextIsolation=true / nodeIntegration=false / sandbox=true / webSecurity=true
- [x] 外链白名单：`setWindowOpenHandler` + `will-navigate` 双拦截走 `shell.openExternal`
- [x] CI workflow（`.github/workflows/build.yml`）：Win + macOS matrix，验 `npm run build:smoke` + artifact 存在
- [ ] HMR 验证（留 dev 流程 daily use 验证；`scripts/dev.mjs` 已就位）
- [ ] `npm run build` 产出 unsigned 安装包 → 移交 [FEATURE_010](#feature_010-跨平台安装包unsigned-dev)
- [ ] CI 实际 PR 触发跑通 → 待首个 PR 验证

### 工程注记（非"推迟项"——只是值得记住的工程细节）

- `scripts/dev.mjs` 在子进程 env 显式 `delete env.ELECTRON_RUN_AS_NODE`。若外部 shell 设了这个变量，electron.exe 会退化成 Node，window 不弹——这是 dev 体验级别的陷阱，不是安全问题。
- `dist-electron/package.json` 由 `scripts/build-main.mjs` 自动生成 `{ type: 'commonjs', main: 'main.js' }`——同时满足根 `type: module` 与 Electron 入口探测两个约束。
- `RENDERER_DIST` 用 `path.join(__dirname, '../apps/desktop/dist')`：dev 和 asar 两种模式下，`dist-electron/` 与 `apps/desktop/dist/` 始终是兄弟目录（electron-builder 默认按 `files` glob 保留项目根的目录结构）。**不**改用 `app.getAppPath()`——dev 时它返回 `<root>/dist-electron`，拼出来反而错。
- `apps/desktop/package.json` 移除了 `main` 字段——这个 workspace 不是被 require 的 Node 包，Electron 入口由根 `package.json` + `electron-builder.yml extraMetadata.main` 决定。
- 未捕获异常通过 `sanitizeError()` 只打 `name / message / stack`，不打原对象（避免 `Error.cause` 或自定义字段把 API key 等敏感数据带进日志）。

### Review 修订（2026-05-16，初次实现 review 反馈）

| 修订点 | 来源 | 位置 |
|--------|------|------|
| Dev CSP 把 `unsafe-eval` / `unsafe-inline` 收窄到 `script-src` 内，移出 `default-src` | security-reviewer | `main.ts` applyCsp |
| `setWindowOpenHandler` 只放行 `https://`，禁掉 `http://`（防 LLM 链接经本机协议处理器跳坑） | security-reviewer | `main.ts` |
| `will-navigate` 的 `file://` 放行改为严格匹配 `pathToFileURL(RENDERER_DIST)` 前缀，杜绝 `file:///etc/passwd` 这类逃逸 | security-reviewer | `main.ts` |
| Preload 把 `process.platform` 在 `exposeInMainWorld` 之前取成 const 原子，避免后人顺手扩成 `process.env` | security-reviewer | `preload.ts` |
| `scripts/dev.mjs` 把固定 `setTimeout(1500)` 改成 `wait-on dist-electron/main.js`，消除慢机器/CI 时序竞态 | code-reviewer | `dev.mjs` |
| ESLint `no-restricted-imports` patterns 改成 `['electron', 'electron/*']` 同时拦截子路径 | code-reviewer | `eslint.config.mjs` |
| ESLint renderer override 显式把 `process` / `Buffer` / `__dirname` 等 Node globals 置 `'off'`（flat config globals 是叠加的） | security-reviewer | `eslint.config.mjs` |

---

## FEATURE_002: IPC schema (zod)

> Status: **Completed** (2026-05-16, v0.1.0-alpha.0)
> 12 个 zod schema 单元测试通过；preload allowlist 由 schema 包派生（不再手维护）；renderer 通过 typed `invoke<C>()` 完成 `space.version` 全链路自检。
> CI 升级：`continue-on-error` 已摘除，typecheck + test 均为 hard gate。

### 需求概述

renderer ↔ main 之间所有消息都经 zod schema 校验，杜绝 schema mismatch 与未声明 channel。这是后续所有 feature 安全可靠的基础。

### 影响范围

- 新建：`packages/space-ipc-schema/`（独立包，renderer 与 main 都依赖）
- 修改：preload.ts 加 channel 白名单
- 主目录：`apps/desktop/electron/ipc/`（每个 feature 一个 handler 文件）

### 技术方案

- 单一真理源：`packages/space-ipc-schema/src/channels.ts` 定义所有 channel 名 + payload schema + return schema
- Main `ipcMain.handle(channel, ...)` 时强制 zod parse 入参
- Renderer 调用前不必客户端校验（信任 main 会校验），但**类型从 schema 自动推断**
- 拒绝未注册 channel：preload 的 `invoke` 检查 channel 是否在 allowlist
- 错误处理：schema 失败时 main 返回 `{ ok: false, error: { code: 'SCHEMA_INVALID', details } }`

### 接口契约

```typescript
// packages/space-ipc-schema/src/channels.ts
import { z } from 'zod';

export const channels = {
  'session.create': {
    input: z.object({ projectRoot: z.string(), provider: z.string() }),
    output: z.object({ sessionId: z.string() }),
  },
  'session.send': {
    input: z.object({ sessionId: z.string(), content: z.string() }),
    output: z.object({ ok: z.boolean() }),
  },
  'session.cancel': {
    input: z.object({ sessionId: z.string() }),
    output: z.void(),
  },
  // ... 后续 feature 各自追加
} as const;

export type ChannelName = keyof typeof channels;
export type ChannelInput<T extends ChannelName> = z.infer<typeof channels[T]['input']>;
export type ChannelOutput<T extends ChannelName> = z.infer<typeof channels[T]['output']>;
```

### 实现步骤

1. 建 `packages/space-ipc-schema` 包
2. 定义初始 channel 集（session 生命周期最小集 + provider config + permission）
3. 在 preload 暴露类型安全的 `invoke<C>(channel: C, payload: ChannelInput<C>)`
4. 在 main 写通用 `registerChannel(name, handler)` 工具，自动 zod parse
5. 写单元测试覆盖 schema 校验失败路径
6. 加 CI lint 规则：renderer 调 `kodaxSpace.invoke()` 时 channel 名必须在 schema 中

### 验收标准

- [x] 100% renderer → main channel 都有 zod schema（preload `ALLOWED_INVOKE_CHANNELS` = `INVOKE_CHANNEL_NAMES`，同一真理源）
- [x] Schema 失败时 main 返回 structured error，不静默 throw（`registerChannel` 包装入参/出参/handler 异常三路全 envelope）
- [x] CI 阻断"channel 用了但没在 schema 注册"的 PR（renderer `invoke<C extends InvokeChannelName>(...)` 类型约束 + 强制 typecheck；preload 运行时再兜底）
- [x] Schema 单元测试覆盖至少 1 个正例 + 1 个 schema 失败例（实际 12 例：包含 input/output 正反例、未知 channel、enum 越界、空串校验、envelope 工厂行为）

### 实际产物

| 路径 | 作用 |
|------|------|
| `packages/space-ipc-schema/src/envelope.ts` | `IpcResult<T>` envelope + `IPC_ERROR_CODES` 闭集 + `ok()`/`fail()` 工厂 |
| `packages/space-ipc-schema/src/channels/version.ts` | 第一个 channel（`space.version`），含 input/output zod 定义 |
| `packages/space-ipc-schema/src/channels/index.ts` | `invokeChannels` / `pushChannels` 字面量字典（单一真理源） |
| `packages/space-ipc-schema/src/registry.ts` | `INVOKE_CHANNEL_NAMES` 运行时 Set + `ChannelInput<C>` / `ChannelOutput<C>` 类型派生 |
| `packages/space-ipc-schema/test/registry.test.ts` | 12 个 `node:test` 单元测试 |
| `apps/desktop/electron/ipc/register.ts` | `registerChannel(name, handler)` 通用注册函数（zod parse + envelope 包装） |
| `apps/desktop/electron/ipc/version.ts` | `space.version` 的 main 端实现 |
| `apps/desktop/electron/preload.ts` | 改用 `INVOKE_CHANNEL_NAMES` / `PUSH_CHANNEL_NAMES` 派生 allowlist |
| `apps/desktop/renderer/src/types/global.d.ts` | `invoke<C extends InvokeChannelName>(...)` 类型约束 |
| `apps/desktop/renderer/src/App.tsx` | 真正调用 `bridge.invoke('space.version', undefined)` 并渲染 envelope 结果 |

### 设计选择记录

- **invoke + push 拆两个 map**：discriminated union over `direction` 在 mapped types 里推导成本高、可读差；显式两个 map（即使 `pushChannels` 现在为空）让类型 + 运行时 allowlist 同源派生。
- **出参也 zod parse**：防 "main 端协议漂移"——handler 返回的 shape 若与 schema 不符立即被发现（envelope 给出 `OUTPUT_INVALID`），而不是让 renderer 拿到错乱 shape 后崩在某个 `.access`。
- **不传 error 原对象给 renderer**：`HANDLER_ERROR` 只装 `err.message`，避免 LLM 接入后（FEATURE_003）异常携带的 API key / prompt 渗到 envelope。
- **renderer 只 `import type`**：`apps/desktop/dist/` 经 grep 校验，0 处 zod 运行时痕迹（`ZodError` / `z.object` 命中数均为 0），符合"renderer bundle 不含 LLM/zod 运行时"的版本级目标。
- **Node 内置 `node:test` + `tsx` 跑测**：不再加 jest/vitest 依赖；包级测试用 `node --test --import tsx/esm test/*.test.ts` 直接跑 TypeScript，启动 < 200ms。

### CI 变更

- 摘除 typecheck 的 `continue-on-error`（FEATURE_001 时是占位允许，FEATURE_002 起强制）
- 新增 `npm test` 步骤
- 步骤顺序：`install → build:packages → typecheck → test → build:renderer + build:main → verify artifacts`

---

## FEATURE_003: Main 进程 KodaX runtime 集成

> Status: **Completed** (2026-05-16, v0.1.0-alpha.0) — **adapter 模式落地**
> ManagedSession 接口面统一，F003 阶段交付 MockKodaXSession 实现。
> 等 KodaX 发到 npm（或本仓库 CI 加 sibling checkout 步骤）后，加 RealKodaXSession 实现是一个独立小 chore，**不**需要再改 IPC / Host / UI。
> 33 测试通过（25 schema + 8 host 集成），Mock 端到端事件流跑通。

### 需求概述

Main 进程直接 in-process import `@kodax-ai/coding` 的 stateful API，承担 agent 执行。这是 KodaX Space 的核心架构（[ADR-003](../ADR/ADR-003-kodax-integration-in-process.md)）。

### 影响范围

- 新建：`apps/desktop/electron/kodax-host.ts`（KodaX runtime wrapper）
- 新建：`apps/desktop/electron/ipc/session.ts`（session 生命周期 IPC handlers）
- 修改：`packages/space-ipc-schema` 增加 session 事件 schema
- 依赖：`@kodax-ai/coding`（pin 到某个版本）

### 技术方案

- `kodax-host.ts` 是单例 wrapper，持有 `Map<sessionId, KodaXClient>`
- 用 KodaX 自带的 `FileSessionStorage`，写到 `~/.kodax/sessions/`
- KodaX 事件（`onTextDelta` / `onToolUse` / `onToolResult` / `onComplete` / `onError`）转发给 renderer 的 `session-event` channel（main → renderer 单向）
- Permission 回调走 IPC ask-and-wait pattern（见 FEATURE_007）
- 未捕获异常用 `process.on('uncaughtException')` 兜底，写日志，UI 显示错误 banner
- Session 取消：调用 KodaX `task_stop` tool（KodaX 内核已实现）

### 接口契约

```typescript
// apps/desktop/electron/kodax-host.ts
import { KodaXClient, type KodaXEvents } from '@kodax-ai/coding';

class KodaXHost {
  private clients = new Map<string, KodaXClient>();

  async createSession(opts: { projectRoot: string; provider: string }): Promise<string> { /*…*/ }
  async send(sessionId: string, content: string): Promise<void> { /*…*/ }
  async cancel(sessionId: string): Promise<void> { /*…*/ }
  async resume(sessionId: string): Promise<void> { /*…*/ }
}

// renderer 收到的事件 schema
type SessionEvent =
  | { kind: 'text_delta'; sessionId: string; text: string }
  | { kind: 'tool_call'; sessionId: string; call: ToolCall }
  | { kind: 'tool_result'; sessionId: string; result: ToolResult }
  | { kind: 'complete'; sessionId: string }
  | { kind: 'error'; sessionId: string; error: string };
```

### 实现步骤

1. 拉 `@kodax-ai/coding` 作为依赖（npm link 或 file: 同 parent dev）
2. 实现 `KodaXHost` 单例（main 进程启动时初始化）
3. 接 `ipcMain.handle('session.create', ...)` 等 IPC handlers
4. 把 KodaXEvents 接到 webContents.send('session-event', ...)
5. 写集成测试：起一个 mock provider 跑 1 个 session，验证事件流完整
6. 写 uncaughtException 兜底
7. 写 session 取消路径（清理 KodaXClient + 通知 renderer）

### 验收标准

- [x] 同时跑多 session 互不干扰（KodaXHost.Map<sessionId, ManagedSession>；集成测试覆盖）
- [x] Session 事件流到达 renderer 顺序正确（host.test.ts 验证 thinking → text x N → tool_start → tool_result → iteration_end → session_complete 序列）
- [x] Cancel 后 session 释放（AbortController + dispose；测试覆盖 cancel → session_error('cancelled') → 不再产 session_complete）
- [x] push payload 100% 通过 session.event zod 校验（push.ts 发出前 safeParse；host.test.ts 再次回放校验）
- [ ] Resume 同一 sessionId 从 `~/.kodax/sessions/<id>.jsonl` 恢复成功 → **依赖 Real adapter，跟踪为 chore**
- [ ] CLI 跑同一 sessionId 能读到 Space 写的 JSONL → 同上，依赖 Real adapter

### 实际产物

| 路径 | 作用 |
|------|------|
| `packages/space-ipc-schema/src/channels/session.ts` | 5 invoke (create/send/cancel/list/delete) + 1 push (`session.event` discriminated union) |
| `packages/space-ipc-schema/test/session.test.ts` | 13 个 schema 单元测试 |
| `apps/desktop/electron/kodax/session-adapter.ts` | `ManagedSession` 接口 + `SessionFactory` 工厂签名 |
| `apps/desktop/electron/kodax/mock-session.ts` | `MockKodaXSession` —— 模拟事件流，AbortController 支持 cancel |
| `apps/desktop/electron/kodax/host.ts` | `KodaXHost` 单例，holds `Map<sessionId, ManagedSession>`；事件统一 emit 走 pushToRenderer |
| `apps/desktop/electron/ipc/push.ts` | main → renderer push 工具：channel allowlist + zod 校验 + 静默 fallback |
| `apps/desktop/electron/ipc/session.ts` | 5 个 IPC handler，全部委托给 kodaxHost |
| `apps/desktop/electron/test/host.test.ts` | 8 个端到端集成测试（不依赖 electron 运行时） |
| `apps/desktop/renderer/src/App.tsx` | 加了 "Session event stream" demo section，端到端可视化验证 |

### 设计选择记录

- **adapter 模式而不是直接 import KodaX SDK**：
  - 探测过 `file:../KodaX/packages/coding` 安装链路——本机能跑（npm 符号链接到 KodaX 仓库，KodaX 自己的 node_modules 提供 @kodax-ai/agent 等 sibling deps），但 CI 没 KodaX checkout 必跪。
  - 真正受益：F003 的架构价值（事件转发、session 生命周期、permission 回调挂钩）和 SDK 是真是假**无关**。adapter 让架构独立可测，未来 Real 接入只换一个文件的实现。
- **push channel 命名为 `session.event` 而不是 7 个独立 channel**：discriminated union by kind 让 renderer 端单一订阅点；KodaXEvents 本身有 20+ 回调，一对一展开会让 schema 包/preload allowlist 都肿。
- **session.send 返回 `{ accepted: true }` 字面量而不是 `{ ok: boolean }`**：业务"失败"（session 不存在、并发 send）通过 envelope `HANDLER_ERROR` 走，不通过 payload。让"接受请求"和"业务成功"语义分离。
- **push.ts 用 getter 而不是直接持窗口引用**：dev HMR + 用户重开窗口都会 destroy webContents；getter 在每次 push 时取最新引用，自然容错。
- **AbortController 串起 cancel**：mock-session 内部 sleep 都监听 AbortSignal，cancel 立即 abort 整条事件流——拿来给 Real adapter 套同样模式（KodaXOptions.abortSignal 字段已有）。
- **测试用 stub webContents 而不是 electron**：`push.ts` 只 import type WebContents，运行时把 target getter 注入捕获 stub。这样集成测试在 Node 内跑（无 electron），不需要打开 BrowserWindow。

### 已知限制 / 后续 chore

- **Real adapter 待加**：等 KodaX 发到 npm（或本仓库 CI 加 KodaX sibling checkout），把 `RealKodaXSession` 接进来。范围限于实现 `ManagedSession` 接口，包名 `@kodax-ai/coding` 的 `KodaXClient` + `KodaXEvents`。**不**改 schema / handler / UI。
- **Session 持久化**：当前 in-memory only。`~/.kodax/sessions/<id>.jsonl` 由 KodaXClient 自带的 FileSessionStorage 写——只有 Real adapter 接入才会真正落盘。Mock 不写。
- **并发 send 当前直接 reject**：F003 范围内不排队（"一次跑完才 ACK"语义）。需要 queuing 可以后续在 ManagedSession 实现里改，schema 不变。
- **permission 回调挂钩**：当前 Mock 不需要 permission，handler 已有占位但实际 wire 留到 FEATURE_007（Permission 弹窗）。

---

## FEATURE_004: Provider 配置 GUI + Keychain

### 需求概述

提供 GUI 让用户配置 12 个 LLM provider 的 API key，key 写入 OS keychain。这是 Space 启动后用户第一件做的事。

### 影响范围

- 新建：`apps/desktop/renderer/features/provider/`
- 新建：`apps/desktop/electron/keychain.ts`（keytar 封装）
- 新建：`apps/desktop/electron/ipc/provider.ts`
- 修改：`packages/space-ipc-schema` 加 provider channel

### 技术方案

- 用 `keytar` npm 包，自动用 macOS Keychain / Win Credential Manager / Linux libsecret
- 服务名：`kodax-space`；账号名 = provider id
- API key **永不**经 IPC 明文传到 renderer——renderer 只看 boolean "is configured"
- Main 启动时把 keychain 中的 key 注入 `process.env`（KodaX `getProvider()` 从 env 读）
- Provider 元数据（catalog / capability / 默认模型）从 `@kodax-ai/llm` 的 `KODAX_PROVIDERS` 直接 import（stateless utility，安全）
- 自定义 provider（OpenAI-compatible / Anthropic-compatible）GUI 表单 → 写 `~/.kodax/config.json`

### 接口契约

```typescript
// IPC channels
'provider.list'         → { providers: { id, name, configured: boolean, default: bool }[] }
'provider.setKey'       → { providerId: string, apiKey: string } → { ok: boolean }
'provider.removeKey'    → { providerId: string } → { ok: boolean }
'provider.test'         → { providerId: string } → { ok: boolean, latency?: number, error?: string }
'provider.setDefault'   → { providerId: string } → { ok: boolean }
'provider.addCustom'    → { name, protocol, baseUrl, apiKeyEnv, model } → { ok: boolean }
```

### 实现步骤

1. 装 `keytar`（注意 prebuilt binaries for Electron）
2. 写 `keychain.ts` 封装 set/get/delete
3. UI：12 个 provider 卡片网格，已配 key 的亮起
4. 加密 key 流程：renderer → IPC → main 写 keychain；renderer 不收 key
5. "Test connection" 按钮：main 用 `getProvider(id)` + 一个 minimal LLM call（如 `"Reply: ok"`）
6. 自定义 provider 表单（OpenAI / Anthropic 兼容）
7. 启动时把 keychain 中所有 key 注入 env
8. 单测：keychain mock + key 不进 IPC 校验

### 验收标准

- [ ] 12 个 built-in provider 全可配
- [ ] API key 不出现在 IPC 日志、错误堆栈、renderer DevTools
- [ ] "Test connection" 实测往返 < 3s（成功 case）
- [ ] 卸载 Space 后 keychain entry 仍在（用户决定何时删）
- [ ] 自定义 provider 表单能保存并被 KodaX 识别

---

## FEATURE_005: 项目与 Session 管理 UI

### 需求概述

用户能选择项目（git root 或工作目录）、创建 / 切换 / 列出 session。这是 Code 面板的左侧抽屉。

### 影响范围

- 新建：`apps/desktop/renderer/features/code/SessionList.tsx`
- 新建：`apps/desktop/renderer/features/code/ProjectPicker.tsx`
- 新建：`apps/desktop/electron/ipc/project.ts`、`ipc/session.ts`（扩展）
- 新建：`~/.kodax/space/projects.json` 存储最近项目

### 技术方案

- "Recent projects" 列表 + "Open folder" 按钮（用 `dialog.showOpenDialog`）
- 自动识别 git root（`fs.stat .git` 向上找）
- 每个 session 卡片显示：标题（首条 prompt 前 50 字）/ provider / mode / Work x/y / 最后活动时间
- 标题自动生成：第 1 条 prompt 给 KodaX 内核（用 cheap LLM）生成 ≤ 8 字的总结
- 新 session 时让用户选 provider、reasoning mode、permission mode

### 接口契约

```typescript
'project.list'    → { projects: Project[] }
'project.open'    → { path: string } → { project: Project }
'project.recent.add' → { path: string } → { ok }

'session.list'    → { projectRoot?: string } → { sessions: SessionMeta[] }
'session.delete'  → { sessionId: string } → { ok }
```

### 实现步骤

1. 写 `~/.kodax/space/projects.json` 读写工具
2. ProjectPicker UI：recent + browse
3. SessionList UI：用 Zustand store 镜像 KodaX 事件
4. Session 标题生成（lazy，第 1 条回应后跑）
5. 删除 session（含 KodaX session JSONL 文件）
6. Layout：左侧 ~240px 抽屉，可拖拽宽度

### 验收标准

- [ ] 选项目后 session 列表过滤显示
- [ ] 新 session 创建后立即出现在列表顶
- [ ] Session 标题自动生成 < 5s
- [ ] 跨重启 recent projects 保留
- [ ] 删除 session 同时删 JSONL 文件

---

## FEATURE_006: 对话流 UI + tool call 渲染

### 需求概述

Code 面板的主体——渲染 LLM 流式回答 + tool call 卡片（可折叠）+ tool result。

### 影响范围

- 新建：`apps/desktop/renderer/features/code/ConversationStream.tsx`
- 新建：`apps/desktop/renderer/features/code/MessageBubble.tsx`
- 新建：`apps/desktop/renderer/features/code/ToolCallCard.tsx`
- 新建：`apps/desktop/renderer/features/code/InputBox.tsx`

### 技术方案

- 流式拼接：renderer 收到 `text_delta` 事件 append 到当前 assistant message buffer
- Tool call 折叠卡片：默认收起，展开显示完整命令 / 参数 / 输出
- 不同 tool kind 有不同的精简显示：
  - `bash`: 命令 + 退出码 + 输出前 N 行
  - `read`: path + offset/limit
  - `write`/`edit`: path + 行数变化（点击跳 Files panel）
  - `grep`/`glob`: pattern + 匹配数
- Markdown 渲染（react-markdown + rehype-highlight）
- 代码块语法高亮
- 输入框：多行 textarea，⌘Enter / Ctrl+Enter 发送
- 用户消息与 assistant 消息视觉区分

### 接口契约

```typescript
// 已在 FEATURE_003 中定义 SessionEvent
// renderer 维护 Map<sessionId, Message[]>，每个 message:
type Message = {
  id: string;
  role: 'user' | 'assistant' | 'tool';
  content: ContentBlock[];
  createdAt: number;
};
type ContentBlock =
  | { type: 'text'; text: string }
  | { type: 'tool_call'; toolCall: ToolCall }
  | { type: 'tool_result'; result: ToolResult };
```

### 实现步骤

1. Message store（Zustand slice）
2. ConversationStream：virtualized list (react-virtuoso) for 长对话性能
3. MessageBubble + Markdown
4. ToolCallCard 折叠 + tool kind 精简显示
5. InputBox（基础版，富文本留后续）
6. 滚动到底部按钮 + 自动 scroll to bottom on new message
7. 复制 message / 复制 code block 按钮

### 验收标准

- [ ] 流式回答字符级实时显示无闪烁
- [ ] 长对话（500+ messages）滚动流畅
- [ ] Tool call 卡片折叠/展开秒级响应
- [ ] Code block syntax 高亮覆盖 TS / JS / Python / Bash / JSON / YAML
- [ ] ⌘Enter 发送可用

---

## FEATURE_007: Permission 弹窗组件

### 需求概述

当 KodaX 在 `plan` / `accept-edits` 模式下需要用户批准时，弹一个非阻塞弹窗供用户决策。

### 影响范围

- 新建：`apps/desktop/renderer/features/permission/PermissionModal.tsx`
- 新建：`apps/desktop/electron/ipc/permission.ts`
- 扩展：`kodax-host.ts` 注入 permission 回调

### 技术方案

- KodaX 已有 `confirmTools` / `Allow patterns` 概念——直接对接 [`computeConfirmTools`](../../KodaX/packages/repl/) 等已有 API
- Main 收到 KodaX `onPermissionRequest` → 转 IPC `permission.request` → renderer 弹窗
- 弹窗显示：tool 名 + 参数 + 风险等级 + 原因
- 选项：Deny / Allow once / Always allow `<pattern>` (with checkbox)
- "Always allow" 写入 `~/.kodax/permissions.json`（与 CLI 共享）
- Renderer 答案通过 IPC `permission.answer.<reqId>` 回给 main
- 危险命令（`rm -rf` / `git push --force` / 等）强制 typed confirmation：必须键入 `CONFIRM` 才 enable Allow

### 接口契约

```typescript
'permission.request' (main → renderer, push):
  { reqId: string, sessionId: string, toolCall: ToolCall, risk: 'low'|'medium'|'high'|'danger', reason: string }

'permission.answer'  (renderer → main):
  { reqId: string, decision: 'deny' | 'allow_once' | 'allow_always', pattern?: string }
```

### 实现步骤

1. 接 KodaX `onPermissionRequest` 事件
2. Risk 评估器（基于 tool kind + 参数）
3. PermissionModal UI + 危险命令 typed confirmation
4. 答案回传 IPC pattern
5. Always allow 持久化到 `~/.kodax/permissions.json`
6. 测试：黑名单命令 100% 被强制 typed confirmation

### 验收标准

- [ ] 弹窗 P95 渲染延迟 < 50ms（用户决策时间不算）
- [ ] 拒绝后 KodaX 收到 deny 并中止 tool 调用
- [ ] Always allow 后下次同 pattern 不再弹
- [ ] 黑名单命令（rm -rf 等）100% 要求 CONFIRM 字符串
- [ ] 多个 session 并发请求权限不串

---

## FEATURE_008: Work 进度 + reasoning mode 切换

### 需求概述

顶栏显示 Work 预算 + 当前 H0/H1/H2 模式徽标 + reasoning mode 下拉（off/auto/quick/balanced/deep）。

### 影响范围

- 新建：`apps/desktop/renderer/features/code/TopBar.tsx`
- 新建：`apps/desktop/renderer/features/code/WorkBudget.tsx`
- 扩展：KodaX 事件流加 Work 进度 + harness profile 推送（依赖 KodaX 内核扩展，先用现有可推送字段）

### 技术方案

- KodaX 内核已有 budget 系统（`{ cap, used }`）+ harness profile（H0/H1/H2）
- 当前 KodaX SDK 暴露这些字段（如不全暴露，先用近似：从消息 metadata 取）
- TopBar 三块：
  - 左：当前 provider 下拉（点切换）
  - 中：Work x/200 进度条 + H0/H1/H2 徽标（仅 H1/H2 显示 Round）
  - 右：reasoning mode 下拉
- 切 reasoning mode 重启 session 否？答：不重启，新设置应用于下一条 prompt

### 接口契约

```typescript
'session.setReasoningMode' → { sessionId, mode: 'off'|'auto'|'quick'|'balanced'|'deep' } → { ok }
'session.setProvider'      → { sessionId, providerId } → { ok }

// session-event 增加：
{ kind: 'work_budget'; sessionId; used: number; cap: number }
{ kind: 'harness_profile'; sessionId; profile: 'H0_DIRECT' | 'H1_EXECUTE_EVAL' | 'H2_PLAN_EXECUTE_EVAL' }
```

### 实现步骤

1. 探测 KodaX SDK 当前哪些字段已暴露
2. 接 work_budget 事件（如无现成，从 KodaX `runKodaX` result 中取）
3. TopBar 组件
4. Provider / reasoning 切换 IPC
5. 视觉：H0 灰、H1 蓝、H2 紫；Work 进度条
6. 90% 预算时小红点提示

### 验收标准

- [ ] Work 进度实时更新
- [ ] H0→H1→H2 升级时徽标变更
- [ ] 切 provider 立即对下一条 prompt 生效
- [ ] 切 reasoning mode 立即生效
- [ ] 预算达 90% 触发提示

---

## FEATURE_009: 文件面板（Monaco read-only + diff）

### 需求概述

右侧抽屉显示项目文件树 + 点击文件名打开 Monaco 只读视图 + tool call `write`/`edit` 自动跳到 diff 视图。

### 影响范围

- 新建：`apps/desktop/renderer/features/code/FilePanel.tsx`
- 新建：`apps/desktop/renderer/features/code/FileTree.tsx`
- 新建：`apps/desktop/renderer/features/code/MonacoViewer.tsx`
- 新建：`apps/desktop/electron/ipc/files.ts`（main 提供文件读取）

### 技术方案

- Main 提供 `files.read(path)` IPC，限定在 `Project.rootPath` 子树（防 path traversal）
- Renderer 装 Monaco Editor（`@monaco-editor/react`）
- 文件树：用 `fast-glob` 或简单 `fs.readdir` 递归，cache 在 main
- 文件大小限制：> 5 MB 拒绝读，显示提示
- Diff 视图：Monaco `DiffEditor`，左老右新
- Tool call `write`/`edit` 完成后自动在 FilePanel 高亮目标文件
- 点击 diff 文件名 → 切换 Monaco 到 diff mode

### 接口契约

```typescript
'files.tree'      → { projectRoot: string, depth?: number } → { tree: FileNode[] }
'files.read'      → { path: string } → { content: string, encoding: 'utf-8' | 'base64' }
'files.diff'      → { path: string, beforeRef?: string } → { before: string, after: string }
```

### 实现步骤

1. 装 Monaco Editor（注意 web worker 配置）
2. 文件树 UI + 展开/折叠状态保存
3. Main `files.read` 实现 + path traversal 防护
4. MonacoViewer 组件（read-only mode）
5. DiffEditor 组件
6. tool call 触发自动跳 diff 的事件机制
7. 大文件 > 5MB 拒绝处理

### 验收标准

- [ ] 项目 < 10k 文件树渲染流畅
- [ ] Monaco 打开 < 1MB 文件 < 500ms
- [ ] Diff 视图正确显示 KodaX `edit` tool 的改动
- [ ] Path traversal 攻击被拒（验证 `../../../etc/passwd`）
- [ ] 二进制文件不崩 Monaco（显示 "binary file" 占位）

---

## FEATURE_010: 跨平台安装包（unsigned dev）

### 需求概述

能用 `npm run build` 产出 Windows `.exe` 和 macOS `.dmg` 安装包，**未签名**（v0.1.5 签名）。

### 影响范围

- 新建：`scripts/build.mjs`、`electron-builder.yml`
- 修改：`.github/workflows/build.yml`
- 新建：`resources/icon.{ico,icns,png}`

### 技术方案

- `electron-builder` 配 Win NSIS + macOS DMG
- 暂不签名（用户首启需手动允许"未知开发者"）
- 包含 KodaX SDK：`@kodax-ai/coding` 作为 dependency，bundle 时 `asar` 打包
- 不包含 KodaX Bun 二进制（in-process 模式不需要 spawn）
- 不包含 KodaX-private 任何代码
- Win arm64 留 v0.1.1（仅 x64 先）
- macOS：universal binary（x64 + arm64）

### 接口契约

无外部 API。产出物：

- Win: `dist/KodaX-Space-Setup-0.1.0.exe`
- macOS: `dist/KodaX-Space-0.1.0-universal.dmg`

### 实现步骤

1. 配 electron-builder.yml
2. 准备 icon（icns / ico / png 多分辨率）
3. 配 `npm run build:win`、`build:mac`
4. CI matrix：windows-latest + macos-latest 各跑一次
5. 写 smoke test：装 → 起 → 跑一个 dummy session（mock provider）→ 退出
6. 上传 artifact 到 GitHub Release（手动 trigger，v0.1.0 标签）

### 验收标准

- [ ] Win 10/11 双击安装包 → "Run anyway" → 装到 `%LOCALAPPDATA%\KodaX Space`
- [ ] macOS 双击 dmg → 拖到 Applications → 首启右键 Open（绕过 Gatekeeper）
- [ ] 安装包大小 < 200 MB
- [ ] 卸载完全清理（无残留进程）
- [ ] CI 每 PR 自动 build，artifact 可下载试装
