# KodaX Space v0.1.4 — Power Features

> Target: 2026-Q4 初
> Theme: 多 tab 终端 + 富文件预览 + 更多 Rust 加速器
> Status: Planned
> Definition of Done: 重度用户的"再痛点"清除——多终端、PDF/docx/xlsx 预览、大 diff & fuzzy 提速

## 版本范围

v0.1.0–v0.1.3 已经构成 daily-driver。v0.1.4 服务**重度用户**：

- 长 session 经常需要并行多个终端
- 评审任务遇到 PDF / docx / xlsx 时需要预览
- 大 diff / 大型项目 fuzzy search 性能跃升

## 验收标准（版本级）

- [ ] 内置终端支持多 tab，每 tab 独立 cwd
- [ ] 点击 PDF / docx / xlsx 文件能在 File panel 直接预览
- [ ] 大文件（10MB+）diff 渲染从秒级降到 < 200ms
- [ ] @-mention 文件选择器在大型项目（10k+ 文件）响应 < 50ms
- [ ] 左侧栏从单项目升级为多项目可折叠树 + per-session 实时状态点（F040）

---

## FEATURE_023: 内置终端多 tab

### 需求概述

[FEATURE_011](v0.1.1.md#feature_011-内置终端xtermjs--node-pty-单-tab) 单 tab 升级到多 tab。每 tab 独立 cwd / pty 进程。可改名、重排。

### 影响范围

- 修改：`apps/desktop/renderer/features/terminal/TerminalPane.tsx`
- 修改：`apps/desktop/electron/ipc/terminal.ts`（多 pty 管理）

### 技术方案

- 一个 pty 池（map<ptyId, NodePtyProcess>）
- 每 tab 一个 pty + 独立 xterm 实例
- Tab 头：名 / cwd 提示 / 关闭按钮 / + 按钮
- 切 tab 时只 attach 不杀进程
- "New tab in current session cwd" 快捷动作
- 拖拽 tab 重排

### 接口契约

```typescript
// 复用 v0.1.1 的 IPC，加：
'terminal.list' → { ptys: { id, cwd, name }[] }
'terminal.rename' → { ptyId, name } → { ok }
```

### 实现步骤

1. PtyPool 管理器
2. xterm 多实例 + tab 切换
3. Tab UI
4. 重排（react-dnd 或纯 onMouseDown）
5. cleanup：关 tab 杀对应 pty
6. 数量上限（如 10），超出提示

### 验收标准

- [ ] 同时开 5 个 tab 不互相阻塞
- [ ] 切 tab 后输出历史保留
- [ ] 关 tab 后无僵尸 pty
- [ ] 命名 / 重排 持久化（重启 Space 后恢复）

---

## FEATURE_024: 文件富预览（PDF / docx / xlsx）

### 需求概述

File panel 中点击非代码文件时，渲染对应预览，不只是显示 "binary file"。

### 影响范围

- 新建：`apps/desktop/renderer/features/code/PdfPreview.tsx`
- 新建：`apps/desktop/renderer/features/code/DocxPreview.tsx`
- 新建：`apps/desktop/renderer/features/code/XlsxPreview.tsx`
- 新建：`apps/desktop/electron/ipc/preview.ts`

### 技术方案

- PDF：用 `pdf.js` 在 renderer 渲染
- docx：用 `mammoth` 转 HTML 在 renderer 显示
- xlsx：用 `sheetjs` (`xlsx`) 解析后渲染成 HTML 表格
- pptx：v0.1.4 不做（后续）
- 大文件分块：> 10MB 拒绝预览
- 预览只读；用户编辑请回 IDE

### 接口契约

```typescript
'preview.canPreview' → { path } → { kind: 'pdf'|'docx'|'xlsx'|'image'|'video'|'binary' }
'preview.docx.toHtml' → { path } → { html: string }
'preview.xlsx.toJson' → { path, sheet?: string } → { sheets, data }
// PDF 直接 renderer 读取 blob
```

### 实现步骤

1. 装 pdf.js / mammoth / sheetjs
2. 文件 kind 探测
3. 各 preview 组件
4. main 提供 file blob read（限制 path）
5. 错误兜底（损坏文件、加密 PDF 等）

### 验收标准

- [ ] PDF 翻页流畅，缩放可用
- [ ] docx 渲染保留标题 / 列表 / 表格基本结构
- [ ] xlsx 多 sheet 切换，数据正确
- [ ] 损坏文件不崩 Space
- [ ] > 10MB 文件显示拒绝提示

---

## FEATURE_025: NAPI native-diff

### 需求概述

第二个 Rust 加速器：大文件 diff 计算用 `imara-diff`（rust-analyzer 同款），替换 JS 实现。

### 影响范围

- 新建：`packages/native/diff/`
- 修改：File panel diff 计算调用点

### 技术方案

- napi-rs 项目，依赖 `imara-diff = "0.1"` 或 `similar = "2"`
- API：`diff(before: string, after: string, opts?: DiffOpts) → DiffHunk[]`
- 输出 unified diff hunks，含 line numbers
- 6 平台 build matrix（同 [FEATURE_014](v0.1.1.md#feature_014-napi-native-tokenizer)）
- Fallback：JS 实现（diff 包）

### 接口契约

```typescript
// @kodax-ai/native-diff
export type DiffHunk = { startBefore, countBefore, startAfter, countAfter, lines: DiffLine[] };
export type DiffLine = { kind: 'context' | 'added' | 'removed', text: string };
export function diff(before: string, after: string, opts?: DiffOpts): DiffHunk[];
```

### 实现步骤

1. 创建 napi-rs 项目
2. imara-diff 集成 + JSON 序列化
3. 6 平台 CI
4. npm 发布（同 native-tokenizer pattern）
5. File panel diff 集成 + fallback
6. Benchmark vs JS 实现（10MB / 100MB 文件）

### 验收标准

- [ ] 10MB 文件 diff < 200ms（vs JS ~2s）
- [ ] 100MB 文件 diff < 5s（JS 可能超时）
- [ ] 输出与 git diff 一致（采样验证）
- [ ] Fallback path 在 NAPI 不可用时工作

---

## FEATURE_026: NAPI native-fuzzy

### 需求概述

第三个 Rust 加速器：@-mention 文件选择器 + 命令面板的模糊搜索用 `nucleo`（Helix 同款）。

### 影响范围

- 新建：`packages/native/fuzzy/`
- 新建：@-mention UI（Code 面板输入框中触发）
- 新建：命令面板（⌘K）

### 技术方案

- napi-rs 项目依赖 `nucleo = "0.5"`
- API：`createMatcher() / addCandidates(items) / search(query) → ScoredMatch[]`
- 用法：项目 10k+ 文件名 + 符号名预 index，search 实时调用
- @-mention：输入框打 `@` 触发，弹下拉，箭头键选
- 命令面板：⌘K 唤出，搜 action / file / session

### 接口契约

```typescript
// @kodax-ai/native-fuzzy
export class Matcher {
  addCandidates(items: string[]): void;
  search(query: string, limit?: number): { item: string, score: number }[];
  clear(): void;
}
```

### 实现步骤

1. 创建 napi-rs 项目
2. nucleo 封装
3. 6 平台 build + 发布
4. @-mention UI + Matcher 集成
5. 命令面板 UI（CommandPalette 组件）
6. Benchmark：10k 文件、10k 符号、查询 P99 < 50ms

### 验收标准

- [ ] @-mention 在 10k 文件项目中 P99 < 50ms
- [ ] 命令面板 ⌘K 唤出 < 100ms
- [ ] 搜索结果与 fzf 行为一致（采样验证 fuzzy ranking）
- [ ] 大项目 index 完成耗时 < 2s（10k 文件）

---

## FEATURE_040: 多项目可折叠 session 侧栏 + per-session 状态指示

### 需求概述

当前 [LeftSidebar](../../apps/desktop/renderer/src/shell/LeftSidebar.tsx) 只认单一 `currentProjectPath`，session 列表是扁平的「当前项目」视图，且没有 per-session 实时状态——用户看不出哪个 session 正在跑任务、哪个在等他确认。

对标 **OpenAI Codex 桌面客户端**（IDE 风 `project sidebar → threads/sessions` 层级，每个 thread = 一个 agent 实例，关掉重开 session 全保留）升级左侧栏为：

1. **多项目可折叠树**：顶层列出所有最近项目，每个可 `▸/▾` 折叠，当前项目默认展开 + 高亮。
2. **session 历史折叠**：每项目展开后默认只显示前 N 条（如 5），其余「还有 X 条…」可展开——长历史不撑爆侧栏。
3. **per-session 状态点（仅左栏，不做标签页）**：🟢 运行中 / 🟡 等待用户确认 / 🔴 错误 / 无点=idle。折叠的项目聚合显示运行计数（`🟢2`），一眼看到哪个项目有 agent 在跑。

> 形态决策（用户拍板 2026-06-03）：**只在左栏显示状态点，不引入对话区上方的标签页**——符合「极简且智能」哲学（codex 至今也只有 sidebar、标签页是其社区未满足的需求 [openai/codex#10886](https://github.com/openai/codex/issues/10886)）。多项目与状态都从已有数据**自动呈现**，用户零额外操作。

### 影响范围

- **已有可复用**：
  - [projects/store.ts](../../apps/desktop/electron/projects/store.ts) `ProjectStore` —— 多项目列表数据层（`~/.kodax/space/projects.json`，path/name/lastUsedAt）已存在，多项目地基已就绪。
  - `buildSessionTreeOrder` / `SessionTree`（LeftSidebar.tsx）—— fork 树渲染，作为「项目内 session 列表」直接复用。
  - `RunningPeersPanel`（跨进程 `session.listRunning`）—— 与本 feature 的「本进程内 per-session 状态」互补，不冲突。
- **main 修改**：projects IPC（list/add/remove/setCurrent，若缺补全）；新增「本进程内多 session 状态投影」（track 每个 session 是 running / awaiting / error）。
- **renderer 重构**：LeftSidebar.tsx（单项目 → 多项目树外层 + 项目内 SessionTree）；appStore（`currentProjectPath` 旁加项目列表、折叠状态、跨项目 session 状态 map）。

> 注意：本 feature 与正在进行的 LeftSidebar 改动有重叠，落地时以届时的 LeftSidebar 现状为基线对齐。

### 技术方案

- **项目树**：`ProjectStore.list()` → 顶层项目节点，按 `lastUsedAt` 倒序；`currentProjectPath` 对应节点高亮 + 默认展开。折叠/展开状态持久化到 localStorage（key 如 `kodax-space.expandedProjects`）。
- **项目内 session**：项目展开时 `session.list({ projectRoot })` 拉该项目 sessions，复用 `SessionTree`（fork 树 + pinned/archived/unread + 现有 filter）。默认渲染前 N 条，超出折叠为「还有 X 条…」按钮，点开显示全部。
- **per-session 状态枚举**：`idle | running | awaiting | error`。
  - 当前 active session：复用 appStore 已有的 streaming 事件（running）+ askUser 队列（awaiting）+ 错误事件（error）。
  - 后台 / 其他项目 session：main 维护「活动 session 状态表」，随 SessionEvent 变更 push 给 renderer（或低频轮询投影），renderer 存进跨项目 `sessionStatus` map。
  - 渲染：session 行尾状态点；项目节点聚合该项目下 running session 计数（`🟢N`），折叠时也显示。
- **不做标签页**：对话区上方不加 tab bar。
- **「极简且智能」自检**：多项目自动来自 ProjectStore、状态自动来自事件流——不增加用户操作负担；无新增配置项。

### 接口契约

```typescript
// projects IPC（已有则复用，缺则补）
'projects.list'       → { projects: { path: string; name: string; lastUsedAt: number }[] }
'projects.add'        → { path } → { ok: boolean; project }
'projects.remove'     → { path } → { ok: boolean }
'projects.setCurrent' → { path } → { ok: boolean }   // 切当前项目根

// per-session 状态投影（二选一）
'session.statusList'  → { statuses: { sessionId: string; status: 'idle'|'running'|'awaiting'|'error' }[] }
// 或：在现有 SessionEvent push 流里增加 status 变更事件，renderer 增量更新 sessionStatus map
```

### 实现步骤

1. main：补全 projects IPC（list/add/remove/setCurrent）；实现本进程内 session 状态表（running/awaiting/error 来源于现有 KodaXEvents + askUser）。
2. appStore：加项目列表 state、`expandedProjects` 折叠 state（localStorage 持久化）、跨项目 `sessionStatus` map。
3. LeftSidebar：新增 `ProjectTree` 外层（项目折叠节点）包裹现有 `SessionTree`；当前项目高亮 + 默认展开。
4. session 历史折叠：前 N 截断 + 「还有 X 条…」展开。
5. per-session 状态点 + 项目级运行计数聚合。
6. 持久化：展开项目集合、当前项目。
7. 测试：ProjectTree 渲染与折叠、状态枚举映射、前 N 截断、当前项目高亮、切项目加载对应 session。

### 验收标准

- [ ] 顶层列出所有最近项目（来自 ProjectStore），当前项目默认展开 + 高亮
- [ ] 项目可折叠/展开，折叠状态重启后恢复
- [ ] 每项目 session 历史默认前 N 条 + 「还有 X 条…」可展开全部
- [ ] 运行中 session 显 🟢、等待确认显 🟡、错误显 🔴、idle 无点
- [ ] 折叠的项目显示运行计数（🟢N），一眼看到哪个项目有 agent 在跑
- [ ] 不引入对话区标签页（仅左栏状态点）
- [ ] 切换项目正确加载对应 session 列表，互不串台
- [ ] 单测覆盖 ProjectTree 折叠 / 状态映射 / 前 N 截断 ≥ 80%
