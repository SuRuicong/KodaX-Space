# v0.1.20 and v0.1.31-v0.1.34 - KodaX Capability Catch-up Plan

> Design batch: F081-F094 + F104
> Status: Mixed (`F081-F084` implemented in current working tree; `F104` implemented in current working tree; `F085-F094` deferred to v0.1.31-v0.1.34)
> Source of truth: this file + [../FEATURE_LIST.md](../FEATURE_LIST.md)
> Rebaseline: v0.1.19 is already released; no new planned work is assigned to v0.1.19.

---

## Batch Intent

This batch turns Space from "KodaX in an Electron shell" into a desktop host that keeps up with KodaX as KodaX evolves. The principle is:

**KodaX owns runtime behavior; Space owns visibility, review, recovery, and user control.**

The batch is organized by release:

| Version | Theme                                                                | Features                     |
| ------- | -------------------------------------------------------------------- | ---------------------------- |
| v0.1.20 | Post-release rebaseline + continuity closeout + display language MVP | F081, F082, F083, F084, F104 |
| v0.1.31 | Workflow v2 desktop host                                             | F085, F086, F087             |
| v0.1.32 | Governance surfaces                                                  | F088, F089, F090             |
| v0.1.33 | Intelligent control surfaces                                         | F091, F092, F093             |
| v0.1.34 | Beta hardening + sandbox feasibility                                 | F094                         |

---

## v0.1.21 Lane Reset (2026-06-21)

The next 0.1.x feature slot is reserved for `F103` in `docs/features/v0.1.21.md`: a pinned runtime summary and live detail host inspired by Codex's right-side summary panel.

Planning impact:

- Existing planned `v0.1.21-v0.1.24` capability catch-up features keep their IDs and design text, but move to `v0.1.31-v0.1.34`.
- `v0.1.22-v0.1.30` are intentionally left uncommitted for F103 polish, regression fixes, and urgent SDK catch-up.
- `F085-F094` remain valid; only their release assignment changes.

## Display Language MVP Split (2026-06-22)

`F076` remains the full i18n foundation track for v0.1.34. For v0.1.20, we split out a smaller user-visible MVP as `F104`.

Planning impact:

- `F104` is part of v0.1.20 because language switching is a daily desktop affordance, not just beta hardening.
- `F104` covers a constrained set of high-frequency surfaces: menu bar, Settings, left sidebar, right sidebar headings, and common modal/toast text.
- `F104` does not require the full CI i18n scanner, pseudo-locale, zh-Hant support, or assistant response language preference.
- `F076/F077/F078` stay in v0.1.34 and build on the same locale primitives instead of replacing them.

## Current Implementation Readout (2026-06-18)

This readout turns the post-v0.1.19 plan into executable slices. The main adjustment is that several v0.1.20 ideas contain both Space-owned UI/IPC work and KodaX SDK-gated behavior. v0.1.20 should ship the Space-owned slices first and keep SDK-gated pieces visible instead of pretending they are done.

| Feature                      | Current state                                                                                                                                                                                                      | Planning consequence                                                                                                           |
| ---------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------ |
| F081 Capability ledger       | Initial readout found root/app at `0.1.19`, internal packages stale at `0.1.9`, and version IPC missing KodaX SDK contract/degraded capability state. RC status: versions and diagnostics are aligned to `0.1.20`. | Done. It creates the diagnostic language needed by the rest of v0.1.20.                                                        |
| F082 Repointel control plane | Session-level `repointel_trace` events already flow from the KodaX SDK into Space. The chip and `/repointel` command can show recent trace data. Standalone warm/status APIs are still absent.                     | Split into local trace/doctor/status UI now, and SDK-gated warm control later.                                                 |
| F083 Quick Ask v2            | Quick Ask currently creates a temporary KodaX session with `permissionMode=plan`, then tries to cancel/delete it. It is not a true side query and has no promotion path into Coder/Partner.                        | Add cleanup guarantees and promotion continuity now; keep no-session sideQuery semantics gated on SDK support.                 |
| F084 CLI to Space teleport   | HLD defines file-mediated handoff, but Space has no handoff IPC/watcher yet. `session.listRunning` already gives partial peer-session awareness.                                                                   | Implement Space-side handoff reader/watcher/accept flow first; CLI writer integration can land when the CLI surface is ready.  |
| F104 Display Language MVP    | Locale primitives, persisted `languageMode`, Settings entry, menu-bar switch, and high-frequency chrome coverage are implemented in the current working tree.                                                      | Keep F104 scoped to English/Simplified Chinese display chrome; leave full locale coverage and QA automation to F076/F077/F078. |

---

## External Gap Review Adjustment (2026-06-19)

A cross-product gap review highlighted several useful directions. The planning decision is:

- No new pre-`v0.1.20` feature debt is reopened. `F081-F084` are treated as the current continuity closeout, with SDK-gated gaps recorded in the capability ledger.
- MCP is not a gap. `F039` already covers Space-side lifecycle and tool catalog management; future work is limited to resume-state visibility in `F090`.
- The provider config SSRF/env-injection item was a real `v0.1.19` release note risk, but the current working tree already guards `provider.addCustom`, KodaX `config.json` custom providers, and provider connection tests. It should be tracked as release verification, not as a new planned feature.
- OS-level execution sandboxing is valid strategic work, but it is not inserted into the workflow-v2 sequence. `F094` should produce a feasibility and threat-model checkpoint; implementation belongs with `F099` or a later sandbox-specific feature.
- PR/GitHub collaboration closure is useful, but it should land with connectors and Partner workbench in `v0.2.x`, not in the `v0.1.x` KodaX capability catch-up lane.
- Enterprise SSO/SAML/SCIM remains route-dependent. `v0.2.x` keeps a local-first policy/audit pack; identity federation is deferred until a deliberate enterprise track exists.

---

## SDK 0.7.53 Rebaseline (2026-06-19)

KodaX 0.7.53 is published and Space has moved the root and desktop workspace dependency ranges to `^0.7.53`.

Planning impact:

- `KodaXEvents.onSidecarMessage` is now a v0.1.20 must-consume host observability event. Space maps verifier `revise` / `blocked` messages to typed `sidecar_message` session events and renders them as system notices.
- `KodaXEvents.onTodoDriftWarning` is now a v0.1.20 must-consume nudge. Space maps it to `todo_drift_warning` events and a session-scoped notification; durable timeline/reporting remains F089.
- `kodax sessions dedupe` is CLI maintenance. Keep desktop exposure deferred until a session hygiene or doctor surface has clear user value.
- `initialExtensionState` / `initialExtensionRecords` improve host-owned resume for extension/MCP state, but Space does not own that state yet. Resume-state visibility remains F090 scope.
- AMA iteration/progress cap fixes, synthetic sidecar follow-up identity, and crash-recovery extension snapshots are automatic SDK benefits; no separate Space feature is required.

---

## Release Candidate Readout (2026-06-22)

v0.1.20 is now a release-candidate scope, not an open planning bucket.

Ship scope:

- F081: Done. Capability ledger exists, package versions are aligned to `0.1.20`, and `space.version` exposes SDK/capability state.
- F082: Done for local status/trace/doctor. Standalone warm start/cancel/progress remains SDK-gated and is documented as degraded capability.
- F083: Done for temporary-session lifecycle visibility and Continue in Coder. True no-session `sideQuery` and Partner promotion remain SDK-gated.
- F084: Done for Space-side handoff receiver/inbox/accept/dismiss. CLI/REPL writer remains upstream/CLI integration work.
- F104: Done for Display Language MVP. Full typed locale expansion, pseudo-locale, CI scanner, zh-Hant, and assistant response-language preference remain F076-F080.

Release-readiness fixes folded into this candidate:

- Session completion notifications ignore restored historical prompt timestamps.
- Left-sidebar Settings entry is a single gear + label button, not a misleading `KodaX Space · gateway/settings` status label.
- Titlebar menu popups use opaque floating surfaces so panel text cannot bleed through.
- Provider settings participate in the Display Language MVP.

Pre-release gates:

- Automated validation completed: `npm test --workspaces --if-present`, `npm run typecheck`, `npm run build:smoke`, `npm run build:win`, `npm run smoke:pack`, `npm run smoke:boot`, `git diff --check`.
- Remaining human release smoke: startup sanity, Settings language switch, provider panel, session send/cancel, workflow panel, right sidebar, handoff inbox, and notification toggle.
- Tag/publish only after the packaged artifact boots and the release notes match `CHANGELOG.md`.

---

## v0.1.20 Execution Plan

### Slice 1: F081 Version and Capability Diagnostics

Scope:

- Add `docs/KODAX_CAPABILITY_LEDGER.md`.
- Align package/version docs or explicitly explain why internal packages are still `0.1.9`.
- Extend version diagnostics with KodaX SDK package/version, supported contract level, and degraded capability flags.
- Link About/Settings diagnostics back to the capability ledger or an exported diagnostics payload.

Acceptance:

- A developer or user can answer "which KodaX capability is Space consuming?" without reading source.
- Version drift is either fixed or intentionally documented.
- Repointel warm, Quick Ask sideQuery, and CLI teleport appear as explicit capability states.

### Slice 2: F082 Repointel Local Doctor and Trace Surface

Scope:

- Keep the existing session `repointel_trace` event path as the source of truth.
- Add a compact trace detail view from the chip and `/repointel` output.
- Add `repointel.status` or equivalent IPC that reports local availability, last trace, current project eligibility, and whether warm is SDK-supported.
- Show disabled warm controls with a precise reason until KodaX exposes a public warm API.

Acceptance:

- A project can show `available`, `idle`, `traced`, `degraded`, or `unavailable` without running a hidden warm action.
- The last trace explains mode, engine, bridge, latency, cache hit, and failure reason when present.
- No Space code reads private Repointel storage.

### Slice 3: F083 Quick Ask Continuity Before True Side Query

Scope:

- Harden temporary session lifecycle: created, completed, cancelled, deleted, and orphaned states should be visible in logs/diagnostics.
- Add "Continue in Coder" from Quick Ask, creating or opening a normal session seeded with the question and answer.
- Add "Continue in Partner" only as a visible planned/degraded action until Partner ingestion semantics are settled.
- Preserve current no-tools/plan-mode behavior until KodaX exposes true `sideQuery`.

Acceptance:

- Closing Quick Ask does not leave ambiguous hidden sessions.
- A useful Quick Ask answer can become a normal Coder session with provenance.
- The UI does not claim no-session semantics until the SDK actually provides them.

### Slice 4: F084 Space-Side Handoff Receiver

Scope:

- Define a handoff JSON schema for `~/.kodax/handoffs/<handoff-id>.json`.
- Add handoff IPC for `list`, `watch`, `accept`, `dismiss`, and stale/error states.
- Accept handoffs by validating workspace access and opening/resuming the referenced session through existing session IPC.
- Use fixture handoff files for manual verification before requiring CLI writer support.

Acceptance:

- A valid handoff file appears in Space and opens the matching session or gives a precise recoverable error.
- Invalid, stale, inaccessible, or mismatched-workspace handoffs are not silently ignored.
- Accepting a handoff never duplicates or rewrites KodaX session truth.

### Slice 5: F104 Display Language MVP

Scope:

- Add a persisted `languageMode: 'system' | 'zh-CN' | 'en-US'` setting and effective-locale resolver.
- Add `Settings > Preferences > Language` with `Follow system`, `简体中文`, and `English`.
- Add menu-bar quick switching under `View > Language`, backed by the same setting.
- Cover high-frequency app chrome: menu labels/items, Settings modal, left sidebar labels/actions, right sidebar tab/headings, and common modal/toast strings.
- Keep model responses, tool output, file contents, provider/model names, paths, command output, and artifacts untranslated.

Acceptance:

- A clean install follows the OS preferred UI language when it is Simplified Chinese, otherwise English.
- Users can switch to Simplified Chinese or English from Settings and from the menu bar without restarting.
- The chosen language persists across restart.
- The covered surfaces do not mix languages except for proper nouns, paths, provider/model names, and runtime/user/generated content.
- The menu-bar language item shows the checked effective selection and does not create a second preference state.

### SDK Dependency Gates

| Gate                             | Blocks                                          | v0.1.20 behavior before gate opens                                                         |
| -------------------------------- | ----------------------------------------------- | ------------------------------------------------------------------------------------------ |
| Public Repointel warm/status API | Real warm start/cancel/progress control in F082 | Show status from local trace/project availability and disable warm with explanation.       |
| KodaX `sideQuery` or equivalent  | True no-session Quick Ask in F083               | Keep temporary plan-mode session and make lifecycle/provenance explicit.                   |
| CLI/REPL handoff writer          | End-to-end F084 teleport from terminal          | Ship Space-side watcher/accept flow against fixture files and future CLI payload contract. |

### Recommended Order

1. F081 first, because it creates the capability ledger and diagnostics vocabulary.
2. F082 second, because current trace events already exist and can be made useful without waiting on warm APIs.
3. F083 third, because it improves daily UX while keeping the current SDK workaround honest.
4. F084 fourth, because the receiver can be implemented independently but end-to-end value depends on CLI writer timing.
5. F104 fifth or in parallel with renderer polish, because it is mostly host/settings/UI infrastructure and should not block SDK-gated runtime work.

---

## FEATURE_081: KodaX Capability Sync Ledger and SDK Contract Watchlist

### Problem

Space docs and implementation can drift from KodaX. A concrete example already happened: workflow host attribution was once documented as an SDK gap, but KodaX later shipped `hostMetadata` and Space started using it while older docs still described the gap.

### Design

Add a living capability ledger that maps KodaX features to Space consumption status.

Recommended location:

```text
docs/KODAX_CAPABILITY_LEDGER.md
```

Ledger fields:

| Field           | Meaning                                                                  |
| --------------- | ------------------------------------------------------------------------ |
| KodaX feature   | Upstream feature ID, version, and design link.                           |
| SDK/API surface | Import path, type, event, or command Space depends on.                   |
| Space status    | `not-needed`, `blocked`, `planned`, `partial`, `consumed`, `deprecated`. |
| Space feature   | F-id that implements the desktop surface.                                |
| Contract risk   | Compatibility, persistence, permission, or UX risk.                      |
| Verification    | Build/test/manual check proving the contract still works.                |

### UX

This feature is mostly internal, but it should surface in Settings or About as a diagnostics link: current KodaX package version, supported SDK contract level, and known degraded capabilities.

### Acceptance

- A capability ledger exists and links KodaX F211/F231/F232/F235/F233/F224/F228/F105/F113/F139 to Space features.
- README, PRD, HLD, and FEATURE_LIST do not disagree on current package/version status.
- Every future KodaX SDK bump includes a short ledger update or an explicit "no Space impact" note.

Implemented 2026-06-18:

- Added `docs/KODAX_CAPABILITY_LEDGER.md`.
- Extended `space.version` with KodaX SDK version, dependency spec, capability contract, and capability states.
- Aligned root, desktop, IPC schema, UI kit, and lockfile versions to the 0.1.20 release candidate baseline.
- Aligned the desktop workspace KodaX dependency range with the installed 0.7.53 SDK baseline.
- Added Space-side consumption of KodaX 0.7.53 sidecar verifier and todo-drift host callbacks through typed session events, renderer notices, and notifications.

---

## FEATURE_082: Repointel Doctor / Warm / Trace Control Plane

### Problem

Repointel is one of Space's core differentiators, but the current plan still treats status/warm as partial. Users need to know whether repo intelligence is installed, warmed, degraded, or unavailable, without learning daemon internals.

### Design

Create a compact Repointel control plane:

- status chip in Coder chrome,
- project-level warm action,
- doctor panel for install/path/daemon/cache/index health,
- trace viewer for the last context-pack/preturn decision,
- graceful fallback explanation when Repointel is missing.

Space only calls public KodaX/Repointel status and control APIs. It does not read private Repointel storage directly.

### Acceptance

- Opening Repointel status shows project/git/trace availability or a clear pending state.
- Warm support is shown as SDK-gated with a precise reason; Space does not run a hidden warm action.
- Missing project/git/trace states produce actionable diagnostics.
- A trace view explains recent context-pack/preturn decisions when SDK trace events exist.

Implemented 2026-06-18:

- Added `repointel.status` IPC with project/git/trace/warm diagnostics.
- Split `/repointel status` and `/repointel trace`; warm is explicitly reported as SDK-gated.
- Added status readout to the Repointel chip popover.

---

## FEATURE_083: Quick Ask v2 Continuity and Side Query Semantics

### Problem

Quick Ask should remain a temporary side-query surface, not a hidden chat product. The missing part is continuity: when a temporary answer becomes useful, the user needs a clean path into Coder or Partner without inventing a second session truth.

### Design

Define Quick Ask v2 semantics:

- no tools by default,
- no automatic session persistence,
- optional "Continue in Coder" and "Continue in Partner",
- optional "Attach current file/diff/project summary" when invoked from Space,
- sideQuery-style KodaX call once SDK support is available,
- short-lived transcript that expires unless promoted.

Promotion creates a normal KodaX session and records the original Quick Ask prompt/answer as a seed note.

### Acceptance

- Quick Ask can answer without adding a normal Coder session unless promoted.
- Promoting to Coder creates one normal session with provenance.
- Quick Ask never silently writes to repo files or KB.
- Keyboard invocation and dismiss behavior remain fast and predictable.

Implemented 2026-06-18:

- Quick Ask now captures session events locally instead of relying on the global session store.
- Added `Continue in Coder`, which promotes the persisted temporary session into Coder and lets `session.history` restore the transcript.
- True no-session side-query semantics remain blocked on SDK support.

---

## FEATURE_084: CLI to Space Teleport v1

### Problem

Space's PRD promises one session across CLI/REPL/desktop surfaces. Teleport is still deferred, so users cannot reliably move an active terminal session into the desktop review surface.

### Design

Implement file-system mediated handoff:

```text
~/.kodax/handoffs/<handoff-id>.json
```

Handoff payload:

- session id,
- workspace path,
- current branch/worktree if available,
- last known permission mode,
- requested target surface,
- optional focus hints such as file, diff, workflow run, or artifact id.

Space watches or scans handoffs, validates workspace access, then opens the matching session. The CLI/REPL remains source of truth for session data.

### Acceptance

- A valid fixture or future CLI handoff appears in Space and opens the matching persisted session history.
- Invalid or stale handoffs fail with clear errors.
- Opening a handoff never duplicates or rewrites session truth.
- Focus hints are parsed later; v0.1.20 receiver preserves session truth first.

Implemented 2026-06-18:

- Added handoff IPC for `list`, `accept`, `dismiss`, and `handoff.changed` watcher push.
- Added Space titlebar handoff inbox UI.
- Accepting a handoff first verifies the matching persisted session through existing `session.list`/history paths, then removes only the handoff descriptor with a session-id guard.

---

## FEATURE_104: Display Language MVP

### Problem

Users need an obvious way to switch KodaX Space between Simplified Chinese and English. The full F076 plan is still correct, but waiting for every surface, pseudo-locale QA, and CI scanner before shipping any language preference leaves the desktop app feeling unfinished for bilingual users.

### Design

F104 is a narrow, user-visible MVP extracted from F076. It ships the preference plumbing and the most visible UI coverage first, without claiming full localization.

Language model:

```ts
type SupportedLocale = 'zh-CN' | 'en-US';
type LanguageMode = 'system' | SupportedLocale;
```

Resolution rules:

1. `zh-CN` and `en-US` explicit modes always win.
2. `system` maps Electron preferred system languages or `navigator.languages` to `zh-CN` when the first supported Simplified Chinese match is present.
3. Other languages, empty values, `C`, and `POSIX` resolve to `en-US`.

Settings and IPC:

- Extend Space settings with `languageMode`, defaulting to `system`.
- Add `settings.setLanguageMode`.
- `settings.get` returns both stored `languageMode` and resolved `effectiveLocale`.
- Renderer may cache the last language mode for first paint, but main process settings remain source of truth.

UI entry points:

- Canonical entry: `Settings > Preferences > Language`.
- Quick entry: top menu `View > Language`.
- Both entries write the same setting and show the same three options: `Follow system`, `简体中文`, `English`.

MVP coverage:

- menu bar labels and menu item labels,
- Settings modal shell, tabs, and Preferences sections,
- left sidebar project/session chrome and empty states,
- right sidebar tab/headings for environment, live/runtime, changes, tasks/workers/plan panels,
- common modal/toast strings for close, save, cancel, open settings, permission confirmation, and basic error/success feedback.

Explicitly out of scope:

- assistant response language preference (F080),
- zh-Hant support (F079),
- pseudo-locale QA (F077),
- required i18n scanner/CI enforcement (F078),
- translating model output, user content, tool output, file contents, paths, provider/model names, command text, logs, or artifacts.

### Acceptance

- Fresh install defaults to `system` and resolves Chinese OS preferences to Simplified Chinese.
- Users can switch language from Settings and from `View > Language`.
- Switching updates covered renderer chrome without restart.
- The selected mode persists across app restart.
- Menu quick switching and Settings never drift into separate states.
- Covered surfaces have no obvious English/Chinese mixing except proper nouns and runtime/user-generated content.
- Unsupported/uncategorized UI strings remain acceptable only when listed as post-MVP F076 coverage debt.

### Implementation Notes

Recommended implementation order:

1. Add locale primitives and parity tests for the MVP key set.
2. Add settings schema migration and IPC tests for valid/invalid `languageMode`.
3. Add renderer provider/hook with first-paint cache and main-process reconciliation.
4. Add Settings language section.
5. Add top-menu `View > Language` checked submenu.
6. Migrate MVP surfaces and add a focused render test/smoke check for both locales.

F104 should leave the full F076 architecture intact: dictionary keys, resolver behavior, and Settings storage should be the foundation that F076 expands instead of code that F076 has to replace.

---

## FEATURE_085: Workflow Durable Replay Resume UI

### Problem

KodaX is adding durable workflow replay resume. Space needs to show replay/recovery state instead of treating restarted workflows as ordinary fresh runs.

### Design

Add workflow run states and actions:

- `resumable`,
- `replaying`,
- `replay-complete`,
- `resume-failed`,
- `orphaned`.

Workflow panel should show:

- why a run is resumable,
- last persisted event time,
- expected replay window,
- resume button,
- discard/archive action,
- divergence or missing-artifact warnings.

### Acceptance

- After app restart, resumable workflow runs are discoverable.
- Users can resume or discard from Space.
- Replay progress is visible and does not duplicate child-agent activity in the transcript.
- Failed replay gives a precise reason and next action.

---

## FEATURE_086: Workflow Draft Review and Approval UI

### Problem

Generated workflows are executable plans. KodaX will support generate-without-run and review-before-start; Space should make that review legible before a workflow starts.

### Design

Add a draft workflow review panel:

- intent summary,
- required inputs,
- capability requirements,
- tool and permission envelope,
- file/worktree impact,
- generated capsule metadata,
- risk and trust notes,
- approve/run, save draft, reject, or ask agent to revise.

Do not expose raw generated script as the primary UI, but include an expandable technical view for advanced users.

### Acceptance

- Natural-language workflow creation can stop at draft state.
- User approval is required before trusted-local workflow execution.
- Draft revisions preserve provenance.
- Saved drafts can be launched later with preflight.

---

## FEATURE_087: Workflow Pipeline Timeline and Replay-Aware Rendering

### Problem

Current workflow UI is tree-first. Pipeline workflows and replayed runs need time/phase semantics: stage order, fan-out/fan-in, skipped stages, and replayed events.

### Design

Add a pipeline timeline mode beside the existing tree:

- horizontal or vertical stage timeline,
- fan-out/fan-in grouping,
- per-stage status and elapsed time,
- token and child-agent counts,
- replay badges for restored events,
- artifact output markers.

The timeline reads SDK snapshots/events only; Space does not infer workflow semantics from transcript text.

### Acceptance

- Pipeline workflows are understandable without opening raw logs.
- Replay events are visually distinct from newly emitted events.
- Users can jump from a stage to child-agent detail, artifact, or transcript evidence.

---

## FEATURE_088: Memory Governance Surface

### Problem

Memory and Partner KB are different things. KodaX memory governs reusable agent context and preferences; Partner KB stores source-backed knowledge. Space needs a UI that makes this distinction explicit.

### Design

Create a Memory panel with:

- proposed memories inbox,
- approved memories,
- stale/conflicting memories,
- source/session provenance,
- approve, reject, edit, merge, archive,
- Coder memory versus Partner KB boundary labels.

Memory writes must be user-reviewable unless KodaX explicitly marks them as safe auto-memory under a configured policy.

### Acceptance

- Proposed memory changes are visible before they become durable.
- Users can trace each memory to source session or artifact.
- Partner KB pages are not mixed into memory lists.
- Conflicts and stale entries are surfaced.

---

## FEATURE_089: Todo Timeline and Session Report

### Problem

Space receives todo updates, but todos are still mostly transient UI. KodaX plans a TodoList JSON/CLI surface; Space should turn that into a durable desktop plan view.

### Design

Add a Todo timeline:

- current plan,
- completed items,
- blocked/skipped items,
- timestamped changes,
- source event/session,
- filters by active/completed/blocked,
- session-end report summarizing what changed.

Todos remain KodaX-owned data. Space renders and annotates, but does not invent a parallel task system.

### Acceptance

- Todo state survives session reopen when KodaX exposes durable data.
- Plan popout and Todo panel share one store.
- Session-end report can be copied or saved as artifact.

---

## FEATURE_090: MCP / Extension Resume State Inspector

### Problem

When a resumed session depends on MCP servers or extension state, users need to know whether those capabilities were restored.

### Design

Add per-session extension state:

- restored MCP servers,
- missing MCP servers,
- stale credentials,
- rehydration pending,
- reconnect/retry actions,
- tools that were available in the original run but unavailable now.

This consumes KodaX F211 once available.

### Acceptance

- Resumed sessions show extension/MCP health.
- Missing state does not fail silently.
- Users can reconnect or continue with degraded capabilities.

---

## FEATURE_091: Effort-First Provider Routing UI

### Problem

Provider/model selection is becoming too complex for a matrix. KodaX F233 makes reasoning effort the first-class control; Space should expose that as a task-aware UI.

### Design

Replace matrix-heavy controls with:

- task intent selector or inferred task class,
- effort levels tied to capability metadata,
- provider/model badges,
- "why this route" explanation,
- per-session override,
- fallback when selected provider lacks a capability.
- a clearer autonomy ladder around the existing `plan` / `accept-edits` / `auto` modes, including concise risk copy and the reason a mode may still ask for approval.

### Acceptance

- Users can choose low/standard/high effort without knowing provider-specific flags.
- Space explains selected provider/model/effort in one compact line.
- Unsupported effort/model combinations are impossible or clearly disabled.
- Users can understand the current autonomy level without opening advanced settings or reading slash-command help.

---

## FEATURE_092: Advisor Consult Review Surface

### Problem

KodaX will expose a verifiable advisor consult primitive. Space can make it useful by turning it into a review action for risky work.

### Design

Add "Ask Advisor" actions for:

- plan review,
- diff review,
- workflow draft review,
- artifact/source faithfulness review,
- release note or PR description review.

Advisor output should include:

- verdict,
- evidence references,
- disagreement points,
- model/provider used,
- confidence and limitations.

### Acceptance

- Advisor consult is available from high-risk review surfaces.
- Advisor results are attached to the reviewed object, not dumped into normal chat.
- Users can accept, dismiss, or ask the main session to revise based on advisor feedback.

---

## FEATURE_093: Skill Self-Improvement Review Surface

### Problem

KodaX self-improvement can propose skill changes. A desktop app needs a review queue so users understand how agent behavior is changing.

### Design

Add a Skill Changes panel:

- proposed skill edits,
- diff view,
- generated rationale,
- test/eval result,
- activate, reject, edit, rollback,
- behavior impact summary.

No proposed skill should silently change active behavior unless the user has enabled an explicit policy.

### Acceptance

- Skill changes are reviewable before activation.
- Activation and rollback are explicit.
- Test/eval output is visible when available.
- Active skill version is inspectable.

---

## FEATURE_094: Beta Quality and Release Cohesion

### Problem

Space has shipped quickly, but docs, version labels, release notes, logging, and diagnostics can drift. Beta users need a coherent product state.

### Design

Create a beta readiness pass:

- README/package/docs version alignment,
- release channel labels,
- known capability degradation list,
- app diagnostics export,
- link to KodaX capability ledger,
- provider config security verification for custom `apiKeyEnv` and `baseUrl` guards introduced after `v0.1.19`,
- execution sandbox feasibility note: current trust boundary, platform options, minimum viable worktree sandbox, and explicit non-goals,
- F069 logger integration or explicit dependency,
- smoke checklist for packaged app boot, session open, provider config, permission prompt, workflow panel, artifact panel, Partner tab.

### Acceptance

- Package version, README status, and planning docs agree.
- About/Diagnostics exposes Space version, KodaX version, platform, and enabled capabilities.
- A packaged build has a repeatable smoke checklist.
- Known gaps are documented instead of implied by stale docs.
- Sandbox support is either explicitly marked unsupported with rationale or linked to a concrete `v0.2.x` implementation path.
- Provider guard tests for env injection and SSRF remain green before cutting the beta build.
