# KodaX Space v0.1.5 — Sidebar Overhaul + Review Long-tail Closeout

> Target: 2026-06
> Theme: Sidebar 双侧重塑（多项目 session 树 + 任务态 mission control）+ MCP 管理收尾 + F021 mcpb 安装完整版 + 累积 review follow-up
> Status: Completed（main 累积 13 commit；已 tag）
>
> 2026-06-05 调整：原 F027（代码签名 + notarize）/ F028（隐私政策 + 文档站）从本版本范围**移除**——
> KodaX Space 定位为自家与可信用户使用的工具，不走"陌生人公开 Beta"路径，故签名 + 公开文档站不再是
> 硬前置。如未来定位变更可再立项。

## 版本范围

v0.1.4 之后 main 累积 commit 主线：

- **F039**（Diag）— McpPanel 加 mcp.logs 折叠面板（connect / status / lastError / cachedAt），补齐 MCP 管理完整能力
- **F040**（多项目 session 侧栏）— LeftSidebar 包 ProjectTree：顶层折叠树 + 当前项目高亮 + per-session 状态点（🟢🟡🔴）+ 折叠项目运行计数
- **F041**（右侧栏 mission control）— Plan / Workers / Changes 三节常驻 + 旧 Working folder / Context 降级折叠；StashNotice 横幅退役；新 `project.gitChanges` IPC
- **F021 收尾**（file association + drag-drop）— Electron file association 注册 `.mcpb` / `.dxt`；macOS open-file + Windows/Linux argv handoff；renderer drop 监听；安装前 dialog 确认（防 drive-by）
- **F005**（recent projects allowlist）— `projectStore.assertAllowed` 替换 9 个敏感 IPC handler 的 `validateProjectRoot` 边界检查为 allowlist 校验
- **canonProjectRoot** 共享 util 抽到 schema 包（renderer + main 同一实现，消化 F040/F041 review MED-3）
- **B1 / C3 review follow-up batch**：B1 permission-mode-at-enqueue 已记为 SDK 需求 + 文档化；C3 Monaco light theme + 同屏实例 cap 5 + multi_edit malformed-edit placeholder
- **v0.1.4 patch carryovers** 全 close

## 验收标准（版本级）

- [x] LeftSidebar 多项目折叠 + 状态点正常工作（F040）
- [x] RightSidebar Plan / Workers / Changes 常驻显示 + 旧 StashNotice 已退役（F041）
- [x] mcpb 双击 / 拖拽安装 OS handoff 需 dialog 确认（F021）
- [x] 敏感 IPC handler 必须 allowlist 项目（F005）
- [x] 所有 review 累积 finding 处理完毕（见 commit 历史）

## 不在本版本（已挪走）

- ~~F027 代码签名 + macOS notarize~~ —— 用户决定不需要
- ~~F028 隐私政策 + 文档站~~ —— 用户决定不需要

如 KodaX Space 未来面向"陌生用户"扩散，再立项。

---

## v0.1.5 cycle 内 deferred 项收尾追踪

post-v0.1.4 review 累积的 follow-up 项绝大部分在本 cycle 内已 close。仍 deferred 的：

| 项 | 状态 | 处理路径 |
|---|---|---|
| `enqueueUserPrompt` 时 snapshot `permissionMode` | 阻塞于 SDK | 已记为 SDK 需求；等 SDK 暴露 enqueue metadata 或 onMidTurnDrain hook |
| `session.list` schema 加 `limit?: number` 分页 | 低优 | 当前 N < 1k 不痛；用户 10k+ session 再做 |
| F041 ChangesSection `inFlightRef` 加 pending refetch 队列 | 5s TTL 内无害 | 等 SDK 出 watchProject hook 时一起重做 |
| F022 `autoDownload` 设置开关 | 低优 | SettingsPopover 改造时合并 |
| F022 `installing` flag NSIS cancel 重置 | 低优 | electron-updater API 出 cancel callback 后再处理 |
| F021 mcpb permission 边界（vs 手配 global server）| 待 product 决策 | 写需求文档后再 implement |

---

## 后续版本（v0.1.6+）

- **v0.1.6** F038 已在 main（实际 in v0.1.5 cycle 完成）— sessions 持久化升 SDK 0.7.42+
- **v0.1.7** F039 已在 main（实际 in v0.1.5 cycle 完成）— MCP 管理完整版
- **v0.1.8 / v0.1.9** opencode-benchmark 拆出的 OC-* 与 KX-I-* feature 集群，见 `docs/features/opencode-benchmark.md`
- **v0.2.0** 原计划"公开 Beta"——若不公开则等同长期 maintenance 节奏，按功能聚类继续小版本
